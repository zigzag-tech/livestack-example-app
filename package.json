{
  "name": "create-livestack",
  "version": "0.0.3",
  "scripts": {
    "build": "rimraf dist && unbuild && rsync -av --exclude=node_modules --exclude=.env template dist"
  },
  "bin": {
    "create-livestack": "./dist/index.mjs"
  },
  "type": "module",
  "devDependencies": {
    "@types/prompts": "^2.4.9",
    "prompts": "^2.4.2",
    "rimraf": "^5.0.7",
    "unbuild": "^2.0.0"
  },
  "main": "dist/index.mjs",
  "files": [
    "dist"
  ]
}
